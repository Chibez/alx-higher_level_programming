-- Displays the number of records with id = 89 in the table 'first_table' of the specified database in your MySQL server.
-- The database name should be passed as an argument to the mysql command.

SELECT COUNT(*) FROM first_table WHERE id = 89;
