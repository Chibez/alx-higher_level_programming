-- Lists all rows of the table 'first_table' from the specified database in your MySQL server.
-- All fields will be printed.
-- The database name should be passed as an argument to the mysql command.

SELECT * FROM first_table;
